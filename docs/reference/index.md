---
title: API Reference
description: Landing page for the FsFlow API reference.
---

# API Reference

This page shows the API reference by surface first, with the main `FsFlow` package as the entry point.

The detailed hub below is the first stop. The member pages sit underneath it.

## Read this first

Choose the package hub that matches the runtime shape of the code you are writing, then drill into the member pages beneath it.

<div class="docs-grid">

<section class="docs-card">
<span class="label">FsFlow</span>
<h2><a href="fsflow/">FsFlow hub</a></h2>
<p>Check, Result, Validation, Flow, AsyncFlow, TaskFlow, ColdTask, and the support types used by the main package.</p>
</section>

</div>

<div class="docs-stack">

<section class="docs-card">
<span class="label">Open first</span>
<h3><a href="fsflow/">FsFlow</a></h3>
<p>Start with the main package hub, then move into the Check, Result, Validation, Flow, AsyncFlow, TaskFlow, ColdTask, and support-type pages.</p>
</section>

</div>
